<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">


    <!-- 宽度和高度 -->
    <size
        android:width="@dimen/dimens_perfect_item_title_width"
        android:height="@dimen/dimens_perfect_item_title_height" />

    <!-- 填充的颜色 -->
    <solid android:color="@color/color_padding_chatlist" />

    <!-- 描边 -->
    <stroke
        android:width="0.5dp"
        android:color="@color/color_bg_top_red"
        android:dashGap="0dp"
        android:dashWidth="1dp" />


</shape>