<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:gravity="bottom">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:padding="8dp">

        <ProgressBar
            android:id="@+id/header_view_progressbar"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:layout_centerVertical="true"
            android:layout_marginRight="8dp"
            android:layout_toLeftOf="@+id/header_view_status"
            android:indeterminate="true"
            android:indeterminateDrawable="@drawable/drawable_loading"
            android:visibility="invisible" />

        <ImageView
            android:id="@+id/header_view_arrow"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:layout_centerVertical="true"
            android:layout_marginRight="8dp"
            android:layout_toLeftOf="@+id/header_view_status"
            android:src="@drawable/img_arrow_down" />

        <TextView
            android:id="@+id/header_view_status"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:text="@string/listview_header_hint_normal" />

    </RelativeLayout>

</LinearLayout>